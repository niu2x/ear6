# Local NES ROMs

Place locally owned NES ROMs under `mapper_N/`. This directory is used by ROM
regression tests, while its ROM files and mapper subdirectories are excluded
from version control.

Ear6 does not distribute these files. Developers are responsible for ensuring
that they have the right to use their local content.
